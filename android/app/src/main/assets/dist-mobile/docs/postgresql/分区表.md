# PostgreSQL 分区表

> **符号约定**：`< >` 必填参数 | `[ ]` 可选参数

---

## 范围分区

**创建范围分区表**
`CREATE TABLE <表名> (<列定义>) PARTITION BY RANGE (<列名>)`
```sql
-- 创建范围分区表
CREATE TABLE orders (
    id BIGSERIAL,
    order_date DATE NOT NULL,
    amount DECIMAL(10,2)
) PARTITION BY RANGE (order_date);
```

**添加范围分区**
`CREATE TABLE <分区名> PARTITION OF <父表> FOR VALUES FROM ('<起始>') TO ('<结束>')`
```sql
-- 添加分区
CREATE TABLE orders_2026_q1 PARTITION OF orders
    FOR VALUES FROM ('2026-01-01') TO ('2026-04-01');

CREATE TABLE orders_2026_q2 PARTITION OF orders
    FOR VALUES FROM ('2026-04-01') TO ('2026-07-01');
```

**添加默认分区**
`CREATE TABLE <分区名> PARTITION OF <父表> DEFAULT`
```sql
-- 默认分区
CREATE TABLE orders_default PARTITION OF orders DEFAULT;
```

---

## 列表分区

**创建列表分区表**
`CREATE TABLE <表名> (<列定义>) PARTITION BY LIST (<列名>)`
```sql
-- 创建列表分区表
CREATE TABLE customers (
    id SERIAL,
    name VARCHAR(100),
    region VARCHAR(20)
) PARTITION BY LIST (region);
```

**添加列表分区**
`CREATE TABLE <分区名> PARTITION OF <父表> FOR VALUES IN (<值1>, <值2>[, ...])`
```sql
-- 添加分区
CREATE TABLE customers_east PARTITION OF customers
    FOR VALUES IN ('华东', '华北');

CREATE TABLE customers_south PARTITION OF customers
    FOR VALUES IN ('华南', '西南');
```

---

## 哈希分区

**创建哈希分区表**
`CREATE TABLE <表名> (<列定义>) PARTITION BY HASH (<列名>)`
```sql
-- 创建哈希分区表
CREATE TABLE logs (
    id BIGSERIAL,
    message TEXT,
    created_at TIMESTAMP DEFAULT NOW()
) PARTITION BY HASH (id);
```

**添加哈希分区**
`CREATE TABLE <分区名> PARTITION OF <父表> FOR VALUES WITH (MODULUS <模数>, REMAINDER <余数>)`
```sql
-- 添加哈希分区
CREATE TABLE logs_0 PARTITION OF logs FOR VALUES WITH (MODULUS 4, REMAINDER 0);
CREATE TABLE logs_1 PARTITION OF logs FOR VALUES WITH (MODULUS 4, REMAINDER 1);
CREATE TABLE logs_2 PARTITION OF logs FOR VALUES WITH (MODULUS 4, REMAINDER 2);
CREATE TABLE logs_3 PARTITION OF logs FOR VALUES WITH (MODULUS 4, REMAINDER 3);
```

---

## 分区管理

**添加新分区**
`CREATE TABLE <分区名> PARTITION OF <父表> FOR VALUES FROM ('<起始>') TO ('<结束>')`
```sql
-- 添加新分区
CREATE TABLE orders_2026_q3 PARTITION OF orders
    FOR VALUES FROM ('2026-07-01') TO ('2026-10-01');
```

**分离分区**
`ALTER TABLE <父表> DETACH PARTITION <分区名>`
```sql
-- 分离分区
ALTER TABLE orders DETACH PARTITION orders_2026_q1;
```

**附加分区**
`ALTER TABLE <父表> ATTACH PARTITION <分区名> FOR VALUES FROM ('<起始>') TO ('<结束>')`
```sql
-- 附加分区
ALTER TABLE orders ATTACH PARTITION orders_2026_q1
    FOR VALUES FROM ('2026-01-01') TO ('2026-04-01');
```

**删除分区**
`DROP TABLE <分区名>`
```sql
-- 删除分区（数据也删除）
DROP TABLE orders_2026_q1;
```

---

## 分区裁剪

**查看分区裁剪**
`EXPLAIN SELECT * FROM <表名> WHERE <分区列> <操作符> <值>`
```sql
-- 查询自动裁剪不需要的分区
EXPLAIN SELECT * FROM orders WHERE order_date >= '2026-04-01';
```

**启用分区裁剪**
`SET enable_partition_pruning = ON`
```sql
-- 确认裁剪
SET enable_partition_pruning = ON;
```
