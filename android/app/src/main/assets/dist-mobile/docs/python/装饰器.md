# 装饰器

> **符号约定**：`< >` 必填参数 | `[ ]` 可选参数

---

## 装饰器基础

**函数作为参数：高阶函数**
`def <函数>(<函数参数>): <调用函数>()`

```python
# 函数作为参数
def greet(name):
    return f"Hello, {name}!"

def call_func(func, arg):
    """接收函数作为参数并调用"""
    return func(arg)

print(call_func(greet, "Alice"))  # Hello, Alice!

# 函数作为返回值
def make_greeting(greeting):
    def greet(name):
        return f"{greeting}, {name}!"
    return greet

hello = make_greeting("Hello")
hi = make_greeting("Hi")
print(hello("Alice"))  # Hello, Alice!
print(hi("Bob"))       # Hi, Bob!
```

---

**基本装饰器：修改函数行为**
`def <装饰器>(func): def wrapper(*args, **kwargs): ...`
`@<装饰器>`
`def <函数>(): ...`

```python
# 定义装饰器
def my_decorator(func):
    def wrapper(*args, **kwargs):
        print("Before function execution")
        result = func(*args, **kwargs)
        print("After function execution")
        return result
    return wrapper

# 使用装饰器
@my_decorator
def say_hello(name):
    print(f"Hello, {name}!")

say_hello("Alice")
# Before function execution
# Hello, Alice!
# After function execution

# 等价于
def say_hello(name):
    print(f"Hello, {name}!")

say_hello = my_decorator(say_hello)
```

---

## 实用装饰器示例

**计时装饰器**
`def timer(func): def wrapper(*args, **kwargs): ...`

```python
import time

def timer(func):
    """计算函数执行时间"""
    def wrapper(*args, **kwargs):
        start_time = time.time()
        result = func(*args, **kwargs)
        end_time = time.time()
        print(f"{func.__name__} 执行时间: {end_time - start_time:.4f} 秒")
        return result
    return wrapper

@timer
def slow_function():
    time.sleep(1)
    print("Function executed")

slow_function()
```

---

**日志装饰器**
`def logger(func): def wrapper(*args, **kwargs): ...`

```python
def logger(func):
    """记录函数调用日志"""
    def wrapper(*args, **kwargs):
        args_str = ", ".join([str(a) for a in args] + [f"{k}={v}" for k, v in kwargs.items()])
        print(f"Calling {func.__name__}({args_str})")
        result = func(*args, **kwargs)
        print(f"{func.__name__} returned: {result}")
        return result
    return wrapper

@logger
def add(a, b):
    return a + b

add(3, 5)
# Calling add(3, 5)
# add returned: 8
```

---

**缓存装饰器**
`def cache(func): def wrapper(*args): ...`

```python
def memoize(func):
    """缓存函数结果"""
    cache_dict = {}
    def wrapper(*args):
        if args not in cache_dict:
            cache_dict[args] = func(*args)
        return cache_dict[args]
    return wrapper

@memoize
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

print(fibonacci(100))  # 快速计算大值

# 使用 functools.lru_cache
from functools import lru_cache

@lru_cache(maxsize=128)
def fibonacci_lru(n):
    if n <= 1:
        return n
    return fibonacci_lru(n - 1) + fibonacci_lru(n - 2)

print(fibonacci_lru(100))

# 查看缓存信息
print(fibonacci_lru.cache_info())
```

---

**权限检查装饰器**
`def require_permission(<权限>): def decorator(func): ...`

```python
def require_permission(permission):
    """权限检查装饰器"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            user = kwargs.get("user", {})
            if user.get("permission") != permission:
                raise PermissionError(f"Permission denied: requires {permission}")
            return func(*args, **kwargs)
        return wrapper
    return decorator

@require_permission("admin")
def delete_user(user_id, user=None):
    print(f"User {user_id} deleted")

delete_user(1, user={"permission": "admin"})  # User 1 deleted
# delete_user(1, user={"permission": "user"})  # PermissionError
```

---

## 带参数的装饰器

**三层嵌套装饰器**
`def <装饰器>(<参数>): def decorator(func): def wrapper(*args, **kwargs): ...`

```python
def repeat(n):
    """重复执行函数 n 次"""
    def decorator(func):
        def wrapper(*args, **kwargs):
            results = []
            for i in range(n):
                result = func(*args, **kwargs)
                results.append(result)
            return results
        return wrapper
    return decorator

@repeat(3)
def say_hello(name):
    print(f"Hello, {name}!")
    return f"Greeting {name}"

say_hello("Alice")
# Hello, Alice!
# Hello, Alice!
# Hello, Alice!
```

---

**带多个参数的装饰器**
`def <装饰器>(<参数1>, <参数2>): def decorator(func): ...`

```python
def retry(max_attempts=3, delay=1):
    """重试装饰器"""
    def decorator(func):
        import time
        def wrapper(*args, **kwargs):
            last_exception = None
            for attempt in range(max_attempts):
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    last_exception = e
                    print(f"Attempt {attempt + 1} failed: {e}")
                    if attempt < max_attempts - 1:
                        time.sleep(delay)
            raise last_exception
        return wrapper
    return decorator

@retry(max_attempts=3, delay=1)
def risky_operation():
    import random
    if random.random() < 0.5:
        raise ValueError("Random failure")
    return "Success"

try:
    result = risky_operation()
    print(result)
except Exception as e:
    print(f"All attempts failed: {e}")
```

---

## functools.wraps

**保留原函数信息**
`@functools.wraps(<函数>)`

```python
import functools

def my_decorator(func):
    @functools.wraps(func)  # 保留原函数信息
    def wrapper(*args, **kwargs):
        print("Before function execution")
        result = func(*args, **kwargs)
        print("After function execution")
        return result
    return wrapper

@my_decorator
def example():
    """示例函数"""
    print("Function executed")

example()
print(f"Function name: {example.__name__}")      # example
print(f"Function docstring: {example.__doc__}")  # 示例函数

# 不使用 functools.wraps 的对比
def bad_decorator(func):
    def wrapper(*args, **kwargs):
        return func(*args, **kwargs)
    return wrapper

@bad_decorator
def example2():
    """示例函数"""
    pass

print(example2.__name__)  # wrapper（丢失原函数名）
```

---

## 类装饰器

**使用类作为装饰器**
`class <装饰器类>: def __init__(self, func): ...; def __call__(self, *args): ...`

```python
class CountCalls:
    """统计函数调用次数"""
    def __init__(self, func):
        self.func = func
        self.count = 0
        functools.update_wrapper(self, func)
    
    def __call__(self, *args, **kwargs):
        self.count += 1
        print(f"Call {self.count} of {self.func.__name__}")
        return self.func(*args, **kwargs)

import functools

@CountCalls
def say_hello(name):
    print(f"Hello, {name}!")

say_hello("Alice")
say_hello("Bob")
say_hello("Charlie")
print(f"Total calls: {say_hello.count}")  # 3
```

---

**类装饰器装饰类**
`def <装饰器>(cls): ...`
`@<装饰器>`
`class <类>: ...`

```python
def add_repr(cls):
    """为类添加 __repr__ 方法"""
    def __repr__(self):
        attrs = ", ".join(f"{k}={v!r}" for k, v in self.__dict__.items())
        return f"{cls.__name__}({attrs})"
    cls.__repr__ = __repr__
    return cls

@add_repr
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

person = Person("Alice", 30)
print(person)  # Person(name='Alice', age=30)
```

---

## 装饰器链

**多个装饰器叠加**
`@<装饰器1>`
`@<装饰器2>`
`def <函数>(): ...`

```python
def decorator1(func):
    def wrapper(*args, **kwargs):
        print("Decorator 1 - before")
        result = func(*args, **kwargs)
        print("Decorator 1 - after")
        return result
    return wrapper

def decorator2(func):
    def wrapper(*args, **kwargs):
        print("Decorator 2 - before")
        result = func(*args, **kwargs)
        print("Decorator 2 - after")
        return result
    return wrapper

@decorator1
@decorator2
def my_function():
    print("Function executed")

my_function()
# Decorator 1 - before
# Decorator 2 - before
# Function executed
# Decorator 2 - after
# Decorator 1 - after

# 等价于: my_function = decorator1(decorator2(my_function))
```

---

## 内置装饰器

**@property：属性访问器**
`@property`
`@<属性>.setter`

```python
class Circle:
    def __init__(self, radius):
        self._radius = radius
    
    @property
    def radius(self):
        return self._radius
    
    @radius.setter
    def radius(self, value):
        if value < 0:
            raise ValueError("Radius cannot be negative")
        self._radius = value
    
    @property
    def area(self):
        return 3.14159 * self._radius ** 2

circle = Circle(5)
print(circle.radius)  # 5
print(circle.area)     # 78.53975
circle.radius = 10
print(circle.area)     # 314.159
```

---

**@staticmethod 和 @classmethod**
`@staticmethod def <方法>(...): ...`
`@classmethod def <方法>(cls, ...): ...`

```python
class MathUtils:
    pi = 3.14159265359
    
    @staticmethod
    def add(a, b):
        """静态方法"""
        return a + b
    
    @classmethod
    def circle_area(cls, radius):
        """类方法"""
        return cls.pi * radius ** 2

print(MathUtils.add(3, 5))          # 8
print(MathUtils.circle_area(2))      # 12.56637061436
```

---

**@dataclass：自动生成方法**
`from dataclasses import dataclass`
`@dataclass class <类>: ...`

```python
from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float

p = Point(3.0, 4.0)
print(p)  # Point(x=3.0, y=4.0)
```

---

## 进阶装饰器

**异步装饰器**
`def <装饰器>(func): async def wrapper(*args, **kwargs): ...`

```python
import asyncio
import functools

def async_timer(func):
    """异步函数计时装饰器"""
    @functools.wraps(func)
    async def wrapper(*args, **kwargs):
        start = asyncio.get_event_loop().time()
        result = await func(*args, **kwargs)
        end = asyncio.get_event_loop().time()
        print(f"{func.__name__} 执行时间: {end - start:.4f} 秒")
        return result
    return wrapper

@async_timer
async def slow_async_function():
    await asyncio.sleep(1)
    return "Done"

asyncio.run(slow_async_function())
```

---

**装饰器工厂**
`def <工厂>(<配置>): def decorator(func): ...`

```python
def create_logger(level="INFO"):
    """日志装饰器工厂"""
    def decorator(func):
        import functools
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            print(f"[{level}] Calling {func.__name__}")
            result = func(*args, **kwargs)
            print(f"[{level}] {func.__name__} completed")
            return result
        return wrapper
    return decorator

@create_logger(level="DEBUG")
def debug_function():
    print("Debug function executed")

@create_logger(level="ERROR")
def error_function():
    print("Error function executed")

debug_function()
error_function()
```
